import { PathAttributePipe } from './path-attribute.pipe';

describe('PathAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new PathAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
